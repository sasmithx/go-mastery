package main

// imports bring external packages into the file that u r working
// where actually u need it
import (
	"fmt"
	"math"
)

func main() {
	// packageName.FunctionName -> call a function from a package

	fmt.Println("sqrt(25)", math.Sqrt(25))

}